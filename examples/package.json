{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "main": "examples/index.js",
  "scripts": {
    "build": "npm install ../ && tsc --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ably-labs/models": "file:..",
    "ably": "^1.2.41",
    "tsconfig-paths": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.3.3"
  }
}
